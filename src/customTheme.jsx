import { extendTheme } from "@chakra-ui/react";
const theme = extendTheme({
  colors: {
    primary: "#FF5B00",
    black: "#00001B",
    font: "#F4F4F4",
    lightOrange: "#FFEEE5",
    textFont: "#ACACAC",
  },
});

export default theme;
